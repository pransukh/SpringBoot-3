# application.properties

# Server port
server.port=8080
spring.security.user.name=pran
spring.security.user.password=pran
#H2 database configuration
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
spring.h2.console.enabled=true


spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/order-service
spring.datasource.username = HITMAN
spring.datasource.password = admin
spring.jpa.hibernate.ddl-auto = update

# Hibernate properties
spring.jpa.show-sql=true

# Logging configuration (optional)
logging.level.org.springframework=INFO
logging.level.your.package.name=DEBUG
spring.servlet.multipart.enabled=true

# Set the logging level for the root logger
logging.level.root=INFO

# Set the logging level for specific packages
logging.level.com.org.let=DEBUG

# Specify the pattern for the console log output
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
